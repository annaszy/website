dist: bionic
language: python
install:
  - pip3 install sphinx
script:
  - make html
deploy:
  local_dir: docs/html
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN 
  on:
    branch: master
